<% @today_workout.each do |workout| %>
  <h3><%= link_to 'Today', athlete_workout_path(@athlete, workout.id) %></h3>
  <%= simple_format(workout.workout) %>
  <h5>Coach's Notes</h5>
  <%= simple_format(workout.coach_notes) %>
<% end %>
